{
  "method_name": "put_actions",
  "url": "https://boto3.amazonaws.com/v1/documentation/api/latest/reference/services/personalize-events/client/put_actions.html",
  "description": "Adds one or more actions to an Actions dataset. For more information see Importing actions individually.",
  "parameters": [
    {
      "name": "datasetArn",
      "type": "string",
      "required": true,
      "description": "The Amazon Resource Name (ARN) of the Actions dataset you are adding the action or actions to.",
      "nested_params": []
    },
    {
      "name": "actions",
      "type": "list",
      "required": true,
      "description": "A list of action data. (dict) --- Represents action metadata added to an Action dataset using the PutActions API. For more information see Importing actions individually. The ID associated with the action. properties (string) --- A string map of action-specific metadata. Each element in the map consists of a key-value pair. For example, {\"value\": \"100\"}. The keys use camel case names that match the fields in the schema for the Actions dataset. In the previous example, the value matches the --\u0098VALUE' field defined in the Actions schema. For categorical string data, to include multiple categories for a single action, separate each category with a pipe separator ( |). For example, \\\"Deluxe|Premium\\\".",
      "nested_params": [
        {
          "name": "actionId",
          "type": "string",
          "required": false,
          "description": "The ID associated with the action.",
          "nested_params": []
        },
        {
          "name": "properties",
          "type": "string",
          "required": false,
          "description": "A string map of action-specific metadata. Each element in the map consists of a key-value pair. For example, {\"value\": \"100\"}. The keys use camel case names that match the fields in the schema for the Actions dataset. In the previous example, the value matches the --\u0098VALUE' field defined in the Actions schema. For categorical string data, to include multiple categories for a single action, separate each category with a pipe separator ( |). For example, \\\"Deluxe|Premium\\\".",
          "nested_params": []
        }
      ]
    }
  ],
  "return_structure": [
    {
      "type": null
    }
  ]
}